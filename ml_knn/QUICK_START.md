# KNN 算法项目 - 快速开始指南

## 🚀 5 分钟快速上手

### Step 0: 激活虚拟环境（必须！）

```bash
conda activate mldl_algo311
cd MLDLAlgorithmCode/ml_knn
```

### Step 1: 安装依赖（首次使用）

```bash
pip install -r requirements.txt
```

### Step 2: 运行默认演示

```bash
python main.py
```

### Step 3: 查看结果

```bash
# 查看生成的图表
cd outputs/figures
dir

# 查看指标数据
cd ../metrics
dir
```

---

## 📋 完整命令（复制粘贴即可）

```bash
conda activate mldl_algo311
cd MLDLAlgorithmCode/ml_knn
pip install -r requirements.txt
python main.py
```

---

## 🎯 运行结果

运行成功后，你将看到：

### 控制台输出
```
============================================================
KNN 算法案例项目
============================================================
随机种子: 42
测试集比例: 0.3
默认 K 值: 5

------------------------------------------------------------
1. 加载数据
------------------------------------------------------------
数据源: 合成数据 (synthetic)
✓ 数据加载完成
  训练集: 350 样本, 10 特征
  测试集: 150 样本
  类别数: 3

...（实验运行中）...

============================================================
实验完成！
============================================================
总运行时间: 159.48 秒
```

### 输出文件

**指标文件（4 个 CSV）**
```
outputs/metrics/
├── k_sensitivity.csv          # K 值敏感性分析
├── distance_comparison.csv    # 距离度量对比
├── baseline_comparison.csv    # 基线模型对照
└── scaling_impact.csv         # 标准化影响
```

**图表文件（17 张 PNG）**
```
outputs/figures/
├── k_vs_accuracy.png                  # K 值 vs 准确率曲线
├── decision_boundary_k1.png           # K=1 决策边界
├── decision_boundary_k5.png           # K=5 决策边界
├── decision_boundary_k20.png          # K=20 决策边界
├── distance_comparison.png            # 距离度量对比
├── confusion_matrix_*.png             # 混淆矩阵（7 张）
├── baseline_comparison.png            # 基线模型对比
├── roc_curve_comparison.png           # ROC 曲线
├── nearest_neighbors_sample*.png      # 最近邻可视化（3 张）
└── scaling_comparison.png             # 标准化影响对比
```

---

## 🔧 高级用法

### 使用自己的数据

```bash
# CSV 数据
python main.py --data_path data/my_data.csv --target_col label

# Excel 数据
python main.py --data_path data/my_data.xlsx --target_col target
```

### 运行特定实验

```bash
# 只运行 K 值敏感性分析
python main.py --experiment A1

# 只运行基线模型对照
python main.py --experiment B1
```

### 自定义参数

```bash
python main.py --seed 123 --test_size 0.2 --k_default 7
```

---

## ⚠️ 常见问题

### 问题 1: ModuleNotFoundError

**错误信息：**
```
ModuleNotFoundError: No module named 'sklearn'
```

**原因：** 未激活虚拟环境或未安装依赖

**解决方法：**
```bash
conda activate mldl_algo311
pip install -r requirements.txt
```

### 问题 2: 运行很慢

**原因：** 决策边界可视化计算量较大

**说明：** 这是正常现象，预计运行时间 2-3 分钟

### 问题 3: 找不到虚拟环境

**错误信息：**
```
Could not find conda environment: mldl_algo311
```

**解决方法：** 创建虚拟环境
```bash
conda create -n mldl_algo311 python=3.11 -y
conda activate mldl_algo311
```

---

## 📚 相关文档

- [完整 README](README.md)
- [虚拟环境说明](ENVIRONMENT_NOTE.md)
- [算法规格书](ALGO_SPEC.md)
- [实现设计文档](IMPLEMENTATION_DESIGN.md)

---

## 🎉 成功标志

如果看到以下输出，说明运行成功：

```
✓ 实验 A1 完成！
✓ 实验 A2 完成！
✓ 实验 B1 完成！
✓ 实验 C1 完成！
✓ 实验 C2 完成！
✓ 实验 A3 完成！

============================================================
实验完成！
============================================================
```

---

**祝你使用愉快！** 🚀
